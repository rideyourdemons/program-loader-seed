export type DrillingRecord = { holeId: string; shiftDate: string; fromMeters?: number; toMeters?: number; rigId?: string; crew?: string[]; bitType?: string; rodSize?: string; mudProgram?: string; downtimeMinutes?: number; notes?: string; };

