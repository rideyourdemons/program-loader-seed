/**
 * Shared data file config (browser)
 */
(function() {
  'use strict';

  window.RYD_DATA_FILES = {
    gates: 'gates.json',
    painPoints: 'pain-points.json',
    tools: 'tools.json',
    toolsCanonical: 'tools-canonical.json'
  };
})();
